import { DebounceMousemoveDirective } from './debounce-mousemove.directive';

describe('DebounceMousemoveDirective', () => {
  it('should create an instance', () => {
    const directive = new DebounceMousemoveDirective();
    expect(directive).toBeTruthy();
  });
});
